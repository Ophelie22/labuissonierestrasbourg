{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <main class="container">    
            <div class="col">
                <h3 class="pb-4 mb-3 border-bottom">{{ category.name }}</h3>
                    {#% include 'delegue/list' with { delegues: category.delegues } %#}
            </div>
    </main>
{% endblock %}
