
{% for category in categories %}
    {% include 'category/item.html.twig' with { category } %}
{% endfor %}

<div class="container">
    <div class="row">
        <div class="d-flex align-items-center justify-content-center">
            {#{ knp_pagination_render(categories) }#}
        </div>
    </div>
</div>